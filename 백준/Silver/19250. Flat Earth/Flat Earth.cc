#include <iostream>
#include <vector>
#include <string>
#include <tuple>
#include <stack>
#include <set>
#include <queue>
#include <algorithm>
#include <math.h>
using namespace std;

int main()
{
    long long mi = 1e16;
    ios_base ::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int a, b, c;
    int n;
    int t;
    cin >> t;
    while(t-->0){
        int x, y, z, r, d;
        cin >> x >> y >> z >> r >> a >> b >> c >> d;
        cout << fixed;
        cout.precision(9);
        cout << r*r*3.1415926535<<'\n';
    }

    return 0;
}
